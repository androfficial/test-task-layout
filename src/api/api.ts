export const API_ROOT =
  'https://frontend-test-assignment-api.abz.agency/api/v1';

const page = 1;
const count = 6;

export const DEFAULT_USERS_PATH = `${API_ROOT}/users?page=${page}&count=${count}`;
